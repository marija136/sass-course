{
  "name": "Sass-course",
  "version": "1.0.0",
  "scripts": {
    "start": "npm run gulp && concurrently \"npm run gulp-watch\" \"npm run lite\"",
    "lite": "lite-server",
    "gulp": "gulp",
    "gulp-watch": "gulp watch"
  },
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-sass": "^4.0.1",
    "lite-server": "^2.4.0"
  },
  "dependencies": {
    "concurrently": "^3.6.0"
  }
}
